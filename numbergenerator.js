// Generate a number between a max and min:
// Math.floor(Math.random() * (max-min) + min)

// Generate a number between 6 and 11:
function randomTypeDelay() {
    return (Math.random() * 6) + 5;
};
console.log(randomTypeDelay())